#include <stdio.h>
int main() {
    int number;
    printf("Enter a number: ");
    scanf("%d", &number);
    if (number > 6)
    {
        printf("The number is greater than 6.\n");
    }
    return 0;
}